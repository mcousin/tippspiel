%h1 Ranking
%br/
%table.table
  %tr
    %th Rank
    %th Name
    %th Total points
    %th
  - User.ranking.sort_by {|user, rank| rank}.each do |user, rank|
    %tr
      %td= rank
      %td= link_to user.name, user
      %td= user.total_points
      %td
        - if current_user.admin?
          = link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-mini btn-danger'

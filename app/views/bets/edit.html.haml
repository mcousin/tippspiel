.btn-group
  - Matchday.all.each do |matchday|
    = link_to matchday.description + " / ", matchday_bets_path(matchday) 

- if @matchday
  %h1= @matchday.description
  %h2 Your bets
  = render :partial => "shared/matchday_bets_form"
  %h2 Bet overview
  = render :partial => "shared/matchday_bets_overview"
